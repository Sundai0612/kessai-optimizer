# 決済最適化アプリ

## 開発者について
- プログラミング完全未経験。専門用語には必ず短い説明を添えること
- iPadのブラウザのみで開発。ローカルでのコマンド実行はできない
- コードを読んで正しさを判断できない。よって「テストが通ること」が唯一の検証手段

## このアプリは何か
店舗名を入れると、ユーザーが持っている決済手段の中から
還元率の高い順に「目安」を提示するWebアプリ。

主な利用場面は **「普段行かない店に入ったとき」**。
毎日使うアプリではなく、辞書のように「必要なときに引く」もの。

## 技術構成
- Vite + React + TypeScript
- PWA対応（ホーム画面に追加できる）
- GitHub Pagesに自動デプロイ
- サーバーなし・ログインなし・データベースなし
- ユーザーデータは localStorage のみ

## 設計上の絶対ルール

1. **src/core/ にはUIコードを入れない**
   計算ロジックと型定義のみ。将来React Nativeへ移植するため。
   React、DOM、localStorage、ブラウザAPIへの依存を持ち込まないこと。

2. **還元率データをコードに書かない**
   すべて public/data/ 配下のJSONから読む。
   条件が変わったときJSONの書き換えだけで反映されるようにする。

3. **通信しない**
   位置情報・利用履歴・登録カードを外部に送信しない。すべて端末内で完結。

4. **数値は「目安」として扱う**
   表示は必ず「還元率目安：1.0%」の形式。0.1%単位に丸める。
   実際より高く見積もらない（不明な要素は加算しない）。

## 対象外（作らないもの）
- 証券口座・クレカ積立に関する機能
- ログイン、アカウント、サーバー同期
- 家計簿アプリとのAPI連携

## 進め方
- 一度に複数の変更をせず、1つずつ確認を取りながら進める
- src/core/ を変更したら必ずテストを書き、通ることを確認してから報告する
- 作業後は「何をしたか」を専門用語なしで説明する
