# このプロジェクトについて

## 開発者について
- プログラミング完全未経験。専門用語には必ず説明を添えること
- iPadのブラウザのみで開発。ローカルでのコマンド実行はできない

## 構成
- Vite + React + TypeScript / PWA対応
- GitHub Pagesに自動デプロイ

## 設計上の絶対ルール
- src/core/ には計算ロジックと型定義のみを置く。UIのコードを混ぜない
  （将来スマホアプリに移植する際、core/ をそのまま持っていくため）
- 還元率データは src/core 内にベタ書きせず、public/data/rates.json から取得する
- 位置情報は外部サーバーに送信しない。すべてブラウザ内で完結させる
- ユーザーの登録情報はlocalStorageに保存。サーバーもログインも作らない

## 進め方
- 一度に複数の変更をせず、1つずつ確認を取りながら進める
- ロジックを変更したら、必ずテストを書いて通ること
